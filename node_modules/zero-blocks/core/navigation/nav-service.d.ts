import { ToggleView } from './toggle';
/**
 * Common service to be injected between components where those implementing common
 * ToggleView interface can register and toggle directives can call their methods.
 * TODO: Track currently active? Events?
 */
export declare class NavigationService {
    private navs;
    constructor();
    add(id: string, navItem: ToggleView): void;
    remove(id: string): void;
    get(id: string): ToggleView;
    toggle(id: string, fireEvents?: boolean): Promise<any>;
    open(id: string, fireEvents?: boolean): Promise<any>;
    close(id: string, fireEvents?: boolean): Promise<any>;
}
